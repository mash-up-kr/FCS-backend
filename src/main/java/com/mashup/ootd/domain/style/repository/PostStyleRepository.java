package com.mashup.ootd.domain.style.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.mashup.ootd.domain.style.domain.PostStyle;

public interface PostStyleRepository extends JpaRepository<PostStyle, Long> {

}
